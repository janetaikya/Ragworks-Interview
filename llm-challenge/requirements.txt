# Backend (FastAPI + DB)
fastapi==0.110.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.29
psycopg2-binary==2.9.9   # PostgreSQL driver
alembic==1.13.1          # DB migrations
redis==5.0.3             # Redis client
python-jose[cryptography]==3.3.0  # JWT authentication
passlib[bcrypt]==1.7.4   # password hashing

# Frontend (Streamlit)
streamlit==1.34.0

# LLM Integration
openai==1.30.1           # OpenAI API client
anthropic==0.25.7        # Anthropic Claude API client (if needed)

# RAG (Vector DB + Storage)
qdrant-client==1.8.2
boto3==1.34.106          # for MinIO / S3 storage

# Email Integration (optional bonus feature)
fastapi-mail==1.4.1

# Other utilities
pydantic==2.7.0          # data validation
python-dotenv==1.0.1     # load .env configs
requests==2.31.0
